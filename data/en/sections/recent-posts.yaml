# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 8
  showOnNavbar: true
  numShow: 6
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
